# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
$LOAD_PATH << "."
require "vision_example"

use_frameworks!

platform :ios, '9.3'

target 'VisionUsageExample' do
  # Comment the next line if you don't want to use dynamic frameworks
  visionExample_pods
  # Pods for VisionUsageExample

end
